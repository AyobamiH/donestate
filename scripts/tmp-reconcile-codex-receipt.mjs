import fs from "node:fs";

const executorPath = "apps/mcp-worker/src/executor.ts";
let executor = fs.readFileSync(executorPath, "utf8");

const oldConstants = `export const SANDBOX_RUNTIME_OPTIONS = { sleepAfter: "15m", keepAlive: true, enableDefaultSession: false } as const;\nexport const IMPLEMENTATION_PROCESS_START_ATTEMPTS = 1;\nexport const IMPLEMENTATION_PROCESS_POLL_MS = 2_000;\nexport const IMPLEMENTATION_PROCESS_RECONCILE_ATTEMPTS = 3;\n\nexport function implementationProcessId(runId: string): string {\n  return \`donestate-implement-\${runId}\`;\n}\n`;
const newConstants = `export const SANDBOX_RUNTIME_OPTIONS = { sleepAfter: "15m", keepAlive: true, enableDefaultSession: false } as const;\nexport const IMPLEMENTATION_START_ATTEMPTS = 1;\nexport const IMPLEMENTATION_CONTROL_RECONCILE_ATTEMPTS = 3;\nexport const IMPLEMENTATION_CONTROL_RETRY_BASE_DELAY_MS = 500;\nexport const IMPLEMENTATION_RECEIPT_SCHEMA = "donestate.implementation-receipt.v1";\nexport const IMPLEMENTATION_RECEIPT_DIR = "/workspace/.donestate-control";\n\nexport function implementationReceiptPath(runId: string): string {\n  return IMPLEMENTATION_RECEIPT_DIR + "/implementation-" + runId + ".receipt";\n}\n\nexport function implementationReceiptCommand(): string {\n  return [\n    'receipt_nonce="$DONESTATE_RECEIPT_NONCE"',\n    'receipt_path="$DONESTATE_RECEIPT_PATH"',\n    'receipt_schema="$DONESTATE_RECEIPT_SCHEMA"',\n    'receipt_run_id="$DONESTATE_RECEIPT_RUN_ID"',\n    'receipt_command_digest="$DONESTATE_RECEIPT_COMMAND_DIGEST"',\n    'unset DONESTATE_RECEIPT_NONCE DONESTATE_RECEIPT_PATH DONESTATE_RECEIPT_SCHEMA DONESTATE_RECEIPT_RUN_ID DONESTATE_RECEIPT_COMMAND_DIGEST',\n    'set +e',\n    CODEX_IMPLEMENT_COMMAND,\n    'exit_code=$?',\n    'set -e',\n    'tmp_path="${receipt_path}.tmp.$$"',\n    'printf \'%s\\t%s\\t%s\\t%s\\t%s\\n\' "$receipt_schema" "$receipt_run_id" "$receipt_command_digest" "$exit_code" "$receipt_nonce" > "$tmp_path"',\n    'mv "$tmp_path" "$receipt_path"',\n    'exit "$exit_code"',\n  ].join("\\n");\n}\n\nexport interface ImplementationReceipt {\n  schema: typeof IMPLEMENTATION_RECEIPT_SCHEMA;\n  runId: string;\n  commandDigest: string;\n  exitCode: number;\n  nonce: string;\n}\n\nexport function parseImplementationReceipt(value: string): ImplementationReceipt {\n  const parts = value.trim().split("\\t");\n  if (parts.length !== 5) throw new Error("implementation receipt field count is invalid");\n  const [schema, runId, commandDigest, exitCodeText, nonce] = parts;\n  if (schema !== IMPLEMENTATION_RECEIPT_SCHEMA) throw new Error("implementation receipt schema is invalid");\n  if (!/^[0-9a-f-]{36}$/.test(runId)) throw new Error("implementation receipt run id is invalid");\n  if (!/^[a-f0-9]{64}$/.test(commandDigest)) throw new Error("implementation receipt command digest is invalid");\n  if (!/^(?:0|[1-9][0-9]{0,2})$/.test(exitCodeText)) throw new Error("implementation receipt exit code is invalid");\n  const exitCode = Number(exitCodeText);\n  if (exitCode > 255) throw new Error("implementation receipt exit code is out of range");\n  if (!/^[a-f0-9]{32}$/.test(nonce)) throw new Error("implementation receipt nonce is invalid");\n  return { schema, runId, commandDigest, exitCode, nonce };\n}\n`;
if (executor.split(oldConstants).length !== 2) throw new Error("executor constants context drift");
executor = executor.replace(oldConstants, newConstants);

const oldCloneSignature = `): Promise<Sandbox> {\n  const id = "clone";`;
const newCloneSignature = `): Promise<{ sandbox: Sandbox; sandboxId: string }> {\n  const id = "clone";`;
if (executor.split(oldCloneSignature).length !== 2) throw new Error("clone signature context drift");
executor = executor.replace(oldCloneSignature, newCloneSignature);

const oldRestoredClone = `    return getSandbox(env.Sandbox, sandboxId, SANDBOX_RUNTIME_OPTIONS);`;
const newRestoredClone = `    return { sandbox: getSandbox(env.Sandbox, sandboxId, SANDBOX_RUNTIME_OPTIONS), sandboxId };`;
if (executor.split(oldRestoredClone).length !== 2) throw new Error("restored clone context drift");
executor = executor.replace(oldRestoredClone, newRestoredClone);

const oldSuccessfulClone = `        await journal.settleAction(id, { state: "SUCCEEDED", result });\n        return sandbox;`;
const newSuccessfulClone = `        await journal.settleAction(id, { state: "SUCCEEDED", result });\n        return { sandbox, sandboxId };`;
if (executor.split(oldSuccessfulClone).length !== 2) throw new Error("successful clone context drift");
executor = executor.replace(oldSuccessfulClone, newSuccessfulClone);

const helperStart = executor.indexOf('const IMPLEMENTATION_TERMINAL_STATUSES = new Set(["completed", "failed", "killed", "error"]);');
const helperEnd = executor.indexOf('export async function executeObjective(', helperStart);
if (helperStart < 0 || helperEnd < 0 || helperEnd <= helperStart) throw new Error("implementation helper context drift");
const newHelpers = `async function waitForImplementationControlRetry(attempt: number): Promise<void> {\n  await new Promise<void>((resolve) => setTimeout(resolve, attempt * IMPLEMENTATION_CONTROL_RETRY_BASE_DELAY_MS));\n}\n\nasync function executeImplementationWithReceipt(\n  env: DoneStateEnv,\n  sandbox: Sandbox,\n  sandboxId: string,\n  journal: ExecutionJournal,\n  objective: HostedObjective,\n  repositoryGovernanceRequired: boolean,\n  openaiApiKey: string,\n  githubToken: string,\n): Promise<Sandbox> {\n  const id = "implement";\n  const authority: AuthorityClass = "local_write";\n  if (!objective.authorities.includes(authority)) throw new RunFailure("BLOCKED_AUTHORITY", authority + " authority is required for " + id);\n  if (journal.cancelled()) throw new RunFailure("FAILED_SAFE", "objective was cancelled before the next action");\n\n  const commandDigest = await digest(CODEX_IMPLEMENT_COMMAND);\n  const receiptNonce = crypto.randomUUID().replaceAll("-", "");\n  const receiptPath = implementationReceiptPath(objective.runId);\n  const previousResult = await journal.startAction(id, authority, {\n    schema: "donestate.action-intent.v1",\n    idempotencyKey: actionIdempotency(objective.runId, id),\n    commandDigest,\n    executionMode: "single_foreground_exec_terminal_receipt_v1",\n    startAttempts: IMPLEMENTATION_START_ATTEMPTS,\n    receiptSchema: IMPLEMENTATION_RECEIPT_SCHEMA,\n    receiptPath,\n    receiptNonceDigest: await digest(receiptNonce),\n    sandboxId,\n  });\n  if (previousResult) return getSandbox(env.Sandbox, sandboxId, SANDBOX_RUNTIME_OPTIONS);\n\n  try {\n    await sandbox.mkdir(IMPLEMENTATION_RECEIPT_DIR, { recursive: true });\n  } catch (error) {\n    const detail = redact(error instanceof Error ? error.message : "implementation receipt directory could not be prepared", [openaiApiKey, githubToken]);\n    const result = { reason: "implementation_receipt_directory_unavailable", error: detail, sandboxId };\n    await journal.settleAction(id, { state: "FAILED", result });\n    throw new RunFailure("BLOCKED_CAPABILITY", "implementation receipt directory could not be prepared", result);\n  }\n\n  let raw: Awaited<ReturnType<Sandbox["exec"]>> | null = null;\n  let controlError: string | null = null;\n  try {\n    raw = await sandbox.exec(implementationReceiptCommand(), {\n      cwd: "/workspace/repo",\n      env: {\n        HOME: "/workspace/home",\n        CODEX_API_KEY: openaiApiKey,\n        DONESTATE_OBJECTIVE: implementationPrompt(objective, repositoryGovernanceRequired),\n        DONESTATE_RECEIPT_NONCE: receiptNonce,\n        DONESTATE_RECEIPT_PATH: receiptPath,\n        DONESTATE_RECEIPT_SCHEMA: IMPLEMENTATION_RECEIPT_SCHEMA,\n        DONESTATE_RECEIPT_RUN_ID: objective.runId,\n        DONESTATE_RECEIPT_COMMAND_DIGEST: commandDigest,\n      },\n      timeout: objective.maxDurationMs,\n    });\n  } catch (error) {\n    controlError = redact(error instanceof Error ? error.message : "implementation foreground control operation was interrupted", [openaiApiKey, githubToken]);\n  }\n\n  const diagnostics = raw\n    ? resultRecord(raw, [openaiApiKey, githubToken])\n    : { success: false, error: controlError, stdout: "", stderr: "", truncated: false };\n  let verifiedReceipt: ImplementationReceipt | null = null;\n  let lastControlError: string | null = null;\n\n  for (let attempt = 1; attempt <= IMPLEMENTATION_CONTROL_RECONCILE_ATTEMPTS; attempt += 1) {\n    const reconciled = getSandbox(env.Sandbox, sandboxId, SANDBOX_RUNTIME_OPTIONS);\n    try {\n      const receipt = parseImplementationReceipt(await reconciled.readFile(receiptPath));\n      if (receipt.runId !== objective.runId || receipt.commandDigest !== commandDigest || receipt.nonce !== receiptNonce) {\n        const result = {\n          ...diagnostics,\n          reason: "implementation_receipt_identity_mismatch",\n          sandboxId,\n          receiptSchema: receipt.schema,\n          receiptRunId: receipt.runId,\n          receiptCommandDigest: receipt.commandDigest,\n          receiptNonceMatched: receipt.nonce === receiptNonce,\n          controlError,\n        };\n        await journal.settleAction(id, { state: "AMBIGUOUS", result });\n        throw new RunFailure("AMBIGUOUS_EFFECT", "implementation terminal receipt did not match the durable action intent", result);\n      }\n      verifiedReceipt = receipt;\n      if (receipt.exitCode !== 0) {\n        const result = {\n          ...diagnostics,\n          success: false,\n          exitCode: receipt.exitCode,\n          sandboxId,\n          receiptSchema: receipt.schema,\n          receiptVerified: true,\n          controlError,\n          controlReconcileAttempt: attempt,\n        };\n        await journal.settleAction(id, { state: "FAILED", result });\n        throw new RunFailure("FAILED_SAFE", "implement failed with exit code " + receipt.exitCode, result);\n      }\n\n      const head = await reconciled.exec("git rev-parse HEAD", { cwd: "/workspace/repo" });\n      if (!head.success) throw new Error("post-implementation repository head check failed with exit code " + head.exitCode);\n      const observedHead = head.stdout.trim();\n      const result = {\n        ...diagnostics,\n        success: true,\n        exitCode: 0,\n        sandboxId,\n        receiptSchema: receipt.schema,\n        receiptVerified: true,\n        controlError,\n        controlRecovered: true,\n        controlReconcileAttempt: attempt,\n        postImplementationHead: observedHead,\n      };\n      await journal.settleAction(id, { state: "SUCCEEDED", result });\n      if (observedHead !== objective.baseHeadSha) {\n        throw new RunFailure("BLOCKED_SAFETY", "coding harness changed the repository head directly", {\n          expected: objective.baseHeadSha,\n          actual: observedHead || null,\n        });\n      }\n      return reconciled;\n    } catch (error) {\n      if (error instanceof RunFailure) throw error;\n      lastControlError = redact(\n        error instanceof Error ? error.message : "implementation receipt or repository continuity could not be read",\n        [openaiApiKey, githubToken],\n      );\n      if (attempt < IMPLEMENTATION_CONTROL_RECONCILE_ATTEMPTS) {\n        await waitForImplementationControlRetry(attempt);\n      }\n    }\n  }\n\n  if (verifiedReceipt) {\n    const result = {\n      ...diagnostics,\n      success: true,\n      exitCode: verifiedReceipt.exitCode,\n      sandboxId,\n      receiptSchema: verifiedReceipt.schema,\n      receiptVerified: true,\n      controlError,\n      controlRecovered: false,\n      reason: "post_implementation_repository_continuity_unavailable",\n      lastControlError,\n    };\n    await journal.settleAction(id, { state: "SUCCEEDED", result });\n    throw new RunFailure(\n      "BLOCKED_CAPABILITY",\n      "implementation completed but the repository control plane could not be re-established",\n      result,\n    );\n  }\n\n  const result = {\n    ...diagnostics,\n    sandboxId,\n    receiptSchema: IMPLEMENTATION_RECEIPT_SCHEMA,\n    receiptVerified: false,\n    controlError,\n    reason: "implementation_terminal_receipt_unavailable",\n    lastControlError,\n  };\n  await journal.settleAction(id, { state: "AMBIGUOUS", result });\n  throw new RunFailure(\n    "AMBIGUOUS_EFFECT",\n    "implementation effect could not be reconciled from a terminal receipt",\n    result,\n  );\n}\n\n`;
executor = executor.slice(0, helperStart) + newHelpers + executor.slice(helperEnd);

const oldExecutionStart = `    const sandbox = await clonePublicRepository(env, journal, objective, repositoryPath);\n    activeSandbox = sandbox;`;
const newExecutionStart = `    let { sandbox, sandboxId } = await clonePublicRepository(env, journal, objective, repositoryPath);\n    activeSandbox = sandbox;`;
if (executor.split(oldExecutionStart).length !== 2) throw new Error("execution clone context drift");
executor = executor.replace(oldExecutionStart, newExecutionStart);

const oldImplementationCall = `    await reconcileImplementationProcess(\n      sandbox,\n      journal,\n      objective,\n      repositoryGovernanceRequired,\n      openaiApiKey,\n      githubToken,\n    );\n    const harnessHead = await sandbox.exec("git rev-parse HEAD", { cwd: repositoryPath });\n    if (!harnessHead.success || harnessHead.stdout.trim() !== objective.baseHeadSha) {\n      throw new RunFailure("BLOCKED_SAFETY", "coding harness changed the repository head directly", {\n        expected: objective.baseHeadSha,\n        actual: harnessHead.stdout.trim() || null,\n      });\n    }\n`;
const newImplementationCall = `    sandbox = await executeImplementationWithReceipt(\n      env,\n      sandbox,\n      sandboxId,\n      journal,\n      objective,\n      repositoryGovernanceRequired,\n      openaiApiKey,\n      githubToken,\n    );\n    activeSandbox = sandbox;\n`;
if (executor.split(oldImplementationCall).length !== 2) throw new Error("implementation call context drift");
executor = executor.replace(oldImplementationCall, newImplementationCall);
fs.writeFileSync(executorPath, executor);

const testPath = "apps/mcp-worker/test/executor.test.ts";
let test = fs.readFileSync(testPath, "utf8");
const oldImport = 'import { CHANGED_FILES_COMMAND, CODEX_IMPLEMENT_COMMAND, IMPLEMENTATION_PROCESS_RECONCILE_ATTEMPTS, IMPLEMENTATION_PROCESS_START_ATTEMPTS, PUBLIC_CLONE_MAX_ATTEMPTS, PUBLIC_CLONE_RETRY_BASE_DELAY_MS, SANDBOX_RUNTIME_OPTIONS, decodeChangedFiles, implementationProcessId, implementationPrompt, protectedMaintenancePath, publicCloneCommand, publicCloneRetryDelayMs, publicCloneSandboxId } from "../src/executor";';
const newImport = 'import { CHANGED_FILES_COMMAND, CODEX_IMPLEMENT_COMMAND, IMPLEMENTATION_CONTROL_RECONCILE_ATTEMPTS, IMPLEMENTATION_RECEIPT_SCHEMA, IMPLEMENTATION_START_ATTEMPTS, PUBLIC_CLONE_MAX_ATTEMPTS, PUBLIC_CLONE_RETRY_BASE_DELAY_MS, SANDBOX_RUNTIME_OPTIONS, decodeChangedFiles, implementationPrompt, implementationReceiptCommand, implementationReceiptPath, parseImplementationReceipt, protectedMaintenancePath, publicCloneCommand, publicCloneRetryDelayMs, publicCloneSandboxId } from "../src/executor";';
if (test.split(oldImport).length !== 2) throw new Error("executor test import context drift");
test = test.replace(oldImport, newImport);

const oldProcessTest = `  it("launches Codex once under a deterministic reconcilable process identity", () => {\n    const runId = "post-codex-control-canary";\n    expect(IMPLEMENTATION_PROCESS_START_ATTEMPTS).toBe(1);\n    expect(IMPLEMENTATION_PROCESS_RECONCILE_ATTEMPTS).toBe(3);\n    expect(implementationProcessId(runId)).toBe(\`donestate-implement-\${runId}\`);\n  });\n`;
const newReceiptTests = `  it("launches Codex once inside a terminal-receipt wrapper outside the repository", () => {\n    const runId = "0e3fc377-6ff7-47ba-8cdf-df0a226a7a85";\n    const receiptPath = implementationReceiptPath(runId);\n    const wrapper = implementationReceiptCommand();\n\n    expect(IMPLEMENTATION_START_ATTEMPTS).toBe(1);\n    expect(IMPLEMENTATION_CONTROL_RECONCILE_ATTEMPTS).toBe(3);\n    expect(receiptPath).toBe(\`/workspace/.donestate-control/implementation-\${runId}.receipt\`);\n    expect(receiptPath.startsWith("/workspace/repo/")).toBe(false);\n    expect(wrapper.split(CODEX_IMPLEMENT_COMMAND)).toHaveLength(2);\n    expect(wrapper).toContain("unset DONESTATE_RECEIPT_NONCE");\n    expect(wrapper).toContain('mv "$tmp_path" "$receipt_path"');\n    expect(wrapper).not.toContain("startProcess");\n  });\n\n  it("parses only the exact implementation terminal receipt contract", () => {\n    const runId = "0e3fc377-6ff7-47ba-8cdf-df0a226a7a85";\n    const commandDigest = "a".repeat(64);\n    const nonce = "b".repeat(32);\n    expect(parseImplementationReceipt(\n      \`\${IMPLEMENTATION_RECEIPT_SCHEMA}\\t\${runId}\\t\${commandDigest}\\t0\\t\${nonce}\\n\`,\n    )).toEqual({ schema: IMPLEMENTATION_RECEIPT_SCHEMA, runId, commandDigest, exitCode: 0, nonce });\n    expect(() => parseImplementationReceipt(\n      \`\${IMPLEMENTATION_RECEIPT_SCHEMA}\\t\${runId}\\t\${commandDigest}\\t256\\t\${nonce}\`,\n    )).toThrow("out of range");\n  });\n`;
if (test.split(oldProcessTest).length !== 2) throw new Error("process test context drift");
test = test.replace(oldProcessTest, newReceiptTests);
fs.writeFileSync(testPath, test);

const ledgerPath = "governance/project-ledger.json";
let ledger = fs.readFileSync(ledgerPath, "utf8");

function updateWorkItem(id, nextAction, waitCondition) {
  const pattern = new RegExp(`(\\"id\\":\\"${id}\\"[\\s\\S]*?\\"nextAction\\":\\")([^\\"]*)(\\",\\"waitCondition\\":\\")([^\\"]*)(\\"[\\s\\S]*?\\"evidenceIds\\":\\[)([^\\]]*)(\\])`);
  const match = ledger.match(pattern);
  if (!match) throw new Error(`${id} ledger context drift`);
  let evidenceIds = match[6];
  if (!evidenceIds.includes('"E-028"')) evidenceIds = evidenceIds.trim() ? `${evidenceIds},"E-028"` : '"E-028"';
  ledger = ledger.replace(pattern, `${match[1]}${nextAction}${match[3]}${waitCondition}${match[5]}${evidenceIds}${match[7]}`);
}

updateWorkItem(
  "VERIFY-006",
  "Replace the 0.12.9 background-process registry as the implementation effect oracle with the already-proven sessionless foreground Codex exec wrapped by one atomic terminal receipt outside the repository; after return or control interruption reacquire the same Sandbox ID and reconcile receipt plus repository HEAD with bounded read-only attempts, never relaunch Codex, preserve 0.12.9, RPC, keepAlive, enableDefaultSession:false, Codex 0.150.1, inner command and timeout, then deploy after normal review and launch one fresh successor canary.",
  "Issue 96 / run 0e3fc377-6ff7-47ba-8cdf-df0a226a7a85 is terminal AMBIGUOUS_EFFECT evidence and must not be retried. Clone attempt 2 succeeded, startProcess was acknowledged under deterministic ID donestate-implement-0e3fc377-6ff7-47ba-8cdf-df0a226a7a85, then that identity disappeared before a terminal result could be reconciled; no validation, branch, PR or OpsTruth call occurred. Cloudflare Sandbox 0.12.9 removes terminal process records from memory after attempting /tmp persistence and explicitly allows the record to be lost if that persistence fails, so getProcess identity cannot be DoneState completion authority.",
);
updateWorkItem(
  "VERIFY-004",
  "After the foreground terminal-receipt and same-Sandbox reacquisition repair is reviewed and deployed on the aligned 0.12.9 RPC + keepAlive + sessionless stack, run one fresh consequence-disabled maintenance canary through implementation, post-Codex control continuity, publication, exact-head CI, OpsTruth v2 response and terminal DoneState read-back.",
  "Issue 96 is terminal process-registry ambiguity, not a candidate for retry. The next canary must wait for the isolated terminal-receipt repair; Sandbox version, RPC transport, keepAlive, sessionless execution, Codex 0.150.1 inner command, timeout, PR-only authority and verifier remain unchanged so the result is attributable.",
);

const evidenceMarker = "\n  ]\n}";
if (ledger.split(evidenceMarker).length !== 2) throw new Error("ledger evidence tail context drift");
const evidence = `\n    ,{\n      "id":"E-028","date":"2026-09-03","identity":"Process-identity canary exposed Sandbox 0.12.9 terminal-process observability loss","situation":"PR 95 merged as ce16418f4176906fcf7d3243058bab474631f2fc after exact-head CI 33725650144 passed; post-merge CI 33725840767 and deployment 33725840849 then passed on the aligned Sandbox 0.12.9 RPC + keepAlive + enableDefaultSession false stack. Fresh issue 96 was durably admitted as exactly one maintenance run 0e3fc377-6ff7-47ba-8cdf-df0a226a7a85.","verification":"The signed issue webhook created finding 87023d1e62b0fb2e058b2a8a38a3023a13bfc15f24e78471fbee301e9bb48b14 and queued run 0e3fc377-6ff7-47ba-8cdf-df0a226a7a85. Clone attempt 2 succeeded. The implement action durably recorded one acknowledged deterministic process ID donestate-implement-0e3fc377-6ff7-47ba-8cdf-df0a226a7a85, then settled AMBIGUOUS with reason process_identity_disappeared; the run entered AMBIGUOUS_EFFECT at 2026-09-03T07:05:31.877Z. No validation, branch, pull request, verification snapshot or OpsTruth decision followed. Cloudflare Sandbox 0.12.9 ProcessStore removes terminal records from memory after attempting file persistence and explicitly permits process history loss when that write fails, so process identity is not a durable completion oracle for DoneState. The next isolated repair returns to the sessionless foreground exec that issue 94 proved can complete, wraps the unchanged inner Codex command with an atomic terminal receipt outside /workspace/repo, removes receipt secrets from the child environment before Codex starts, and reacquires the same Sandbox ID for bounded read-only receipt and repository-head reconciliation.",\n      "accountability":{"owner":"DoneState maintainers","status":"active","nextAction":"Review the foreground terminal-receipt and same-Sandbox reacquisition repair, preserve the aligned 0.12.9 RPC + keepAlive + sessionless runtime and unchanged Codex inner command, deploy normally, then launch one fresh successor canary.","waitCondition":"Issue 96 and run 0e3fc377-6ff7-47ba-8cdf-df0a226a7a85 are terminal ambiguity evidence and must not be retried, reset or rewritten. Missing or mismatched receipt must remain AMBIGUOUS_EFFECT and must never authorize a second Codex launch.","staleDate":"2026-09-10"},\n      "outcome":"The background-process idea removed blind retry but did not provide durable terminal identity in Sandbox 0.12.9. The next repair moves effect reconciliation to a DoneState-owned terminal receipt while retaining fail-closed ambiguity when the Sandbox filesystem disappears.","content":"PR 95 merge and CI/deployment subjects, issue 96 finding/run identity, successful clone attempt, acknowledged process identity, process_identity_disappeared settlement, terminal AMBIGUOUS_EFFECT state, absence of publication/verifier activity, Cloudflare 0.12.9 process-store semantics, and the bounded receipt/reacquisition design.","measurement":"One fresh issue webhook, one run, two clone attempts with one successful materialization, exactly one acknowledged implementation launch, one process-identity disappearance, zero validations, zero branches, zero pull requests, zero OpsTruth calls, and zero Codex relaunches."\n    }`;
ledger = ledger.replace(evidenceMarker, evidence + evidenceMarker);
fs.writeFileSync(ledgerPath, ledger);
