import fs from 'node:fs';

const executorPath = 'apps/mcp-worker/src/executor.ts';
let executor = fs.readFileSync(executorPath, 'utf8');

const constantsOld = 'export const SANDBOX_RUNTIME_OPTIONS = { sleepAfter: "15m", keepAlive: true } as const;\n';
const constantsNew = `export const SANDBOX_RUNTIME_OPTIONS = { sleepAfter: "15m", keepAlive: true } as const;\nexport const IMPLEMENTATION_PROCESS_START_ATTEMPTS = 1;\nexport const IMPLEMENTATION_PROCESS_POLL_MS = 2_000;\nexport const IMPLEMENTATION_PROCESS_RECONCILE_ATTEMPTS = 3;\nexport const IMPLEMENTATION_LOG_TAIL_BYTES = 65_536;\nexport const IMPLEMENTATION_STDOUT_PATH = "/workspace/home/donestate-implement.stdout.log";\nexport const IMPLEMENTATION_STDERR_PATH = "/workspace/home/donestate-implement.stderr.log";\n\nexport function implementationProcessId(runId: string): string {\n  return \`donestate-implement-\${runId}\`;\n}\n\nexport function implementationProcessCommand(): string {\n  return \`(\${CODEX_IMPLEMENT_COMMAND}) > \${IMPLEMENTATION_STDOUT_PATH} 2> \${IMPLEMENTATION_STDERR_PATH}\`;\n}\n`;
if (!executor.includes(constantsOld)) throw new Error('executor constants context drift');
executor = executor.replace(constantsOld, constantsNew);

const helperMarker = 'export async function executeObjective(\n';
if (!executor.includes(helperMarker)) throw new Error('executeObjective marker missing');
const helpers = `const IMPLEMENTATION_TERMINAL_STATUSES = new Set(["completed", "failed", "killed", "error"]);\n\nfunction waitForImplementationPoll(delayMs = IMPLEMENTATION_PROCESS_POLL_MS): Promise<void> {\n  return new Promise<void>((resolve) => setTimeout(resolve, delayMs));\n}\n\nasync function implementationLogTail(\n  sandbox: Sandbox,\n  path: string,\n  secrets: string[],\n): Promise<{ text: string; truncated: boolean }> {\n  const tail = await sandbox.exec(\n    \`tail -c \${IMPLEMENTATION_LOG_TAIL_BYTES} \${path} 2>/dev/null || true\`,\n    { timeout: 30_000 },\n  );\n  return boundedOutput(redact(tail.stdout, secrets));\n}\n\nasync function reconcileImplementationProcess(\n  sandbox: Sandbox,\n  journal: ExecutionJournal,\n  objective: HostedObjective,\n  openaiApiKey: string,\n  githubToken: string,\n): Promise<Record<string, unknown>> {\n  const id = "implement";\n  const authority: AuthorityClass = "local_write";\n  if (!objective.authorities.includes(authority)) throw new RunFailure("BLOCKED_AUTHORITY", \`\${authority} authority is required for \${id}\`);\n  if (journal.cancelled()) throw new RunFailure("FAILED_SAFE", "objective was cancelled before the next action");\n\n  const processId = implementationProcessId(objective.runId);\n  const command = implementationProcessCommand();\n  const previousResult = await journal.startAction(id, authority, {\n    schema: "donestate.action-intent.v1",\n    idempotencyKey: actionIdempotency(objective.runId, id),\n    commandDigest: await digest(CODEX_IMPLEMENT_COMMAND),\n    processId,\n    executionMode: "single_launch_reconcilable_process_v1",\n    startAttempts: IMPLEMENTATION_PROCESS_START_ATTEMPTS,\n  });\n  if (previousResult) return previousResult;\n\n  let launchAcknowledged = false;\n  let launchError: string | null = null;\n  try {\n    const process = await sandbox.startProcess(command, {\n      cwd: "/workspace/repo",\n      env: { HOME: "/workspace/home", CODEX_API_KEY: openaiApiKey, DONESTATE_OBJECTIVE: implementationPrompt(objective, objective.objectiveClass === "maintenance_pr") },\n      timeout: objective.maxDurationMs,\n      processId,\n      autoCleanup: false,\n    });\n    if (process.id !== processId) {\n      await journal.settleAction(id, { state: "AMBIGUOUS", result: { processId, observedProcessId: process.id, reason: "process_identity_mismatch" } });\n      throw new RunFailure("AMBIGUOUS_EFFECT", "implementation process identity did not match the durable intent", { processId, observedProcessId: process.id });\n    }\n    launchAcknowledged = true;\n  } catch (error) {\n    if (error instanceof RunFailure) throw error;\n    launchError = redact(error instanceof Error ? error.message : "implementation process launch acknowledgement was lost", [openaiApiKey, githubToken]);\n  }\n\n  const deadline = Date.now() + objective.maxDurationMs + 30_000;\n  let observedProcess = launchAcknowledged;\n  let consecutiveReadFailures = 0;\n  let missingReads = 0;\n\n  while (Date.now() < deadline) {\n    let process: Awaited<ReturnType<Sandbox["getProcess"]>>;\n    try {\n      process = await sandbox.getProcess(processId);\n      consecutiveReadFailures = 0;\n    } catch (error) {\n      consecutiveReadFailures += 1;\n      if (consecutiveReadFailures >= IMPLEMENTATION_PROCESS_RECONCILE_ATTEMPTS) {\n        const detail = redact(error instanceof Error ? error.message : "implementation process could not be re-observed", [openaiApiKey, githubToken]);\n        await journal.settleAction(id, { state: "AMBIGUOUS", result: { processId, reason: "process_observation_unavailable", error: detail } });\n        throw new RunFailure("AMBIGUOUS_EFFECT", "implementation process could not be reconciled after an admitted control interruption", { processId, error: detail });\n      }\n      await waitForImplementationPoll(500 * consecutiveReadFailures);\n      continue;\n    }\n\n    if (!process) {\n      missingReads += 1;\n      if (observedProcess || missingReads >= IMPLEMENTATION_PROCESS_RECONCILE_ATTEMPTS) {\n        const result = { processId, reason: observedProcess ? "process_identity_disappeared" : "launch_acknowledgement_lost_process_not_observable", launchError };\n        await journal.settleAction(id, { state: "AMBIGUOUS", result });\n        throw new RunFailure("AMBIGUOUS_EFFECT", "implementation process identity could not be reconciled", result);\n      }\n      await waitForImplementationPoll(500 * missingReads);\n      continue;\n    }\n\n    observedProcess = true;\n    missingReads = 0;\n    if (!IMPLEMENTATION_TERMINAL_STATUSES.has(process.status)) {\n      await waitForImplementationPoll();\n      continue;\n    }\n\n    const [stdout, stderr] = await Promise.all([\n      implementationLogTail(sandbox, IMPLEMENTATION_STDOUT_PATH, [openaiApiKey, githubToken]),\n      implementationLogTail(sandbox, IMPLEMENTATION_STDERR_PATH, [openaiApiKey, githubToken]),\n    ]);\n    const exitCode = typeof process.exitCode === "number" ? process.exitCode : process.status === "completed" ? 0 : -1;\n    const success = process.status === "completed" && exitCode === 0;\n    const result = {\n      success,\n      exitCode,\n      stdout: stdout.text,\n      stderr: stderr.text,\n      truncated: stdout.truncated || stderr.truncated,\n      processId,\n      processStatus: process.status,\n      launchAcknowledged,\n      launchError,\n    };\n    await journal.settleAction(id, { state: success ? "SUCCEEDED" : "FAILED", result });\n    if (!success) throw new RunFailure("FAILED_SAFE", \`implement process ended in \${process.status} with exit code \${exitCode}\`, result);\n    return result;\n  }\n\n  let process: Awaited<ReturnType<Sandbox["getProcess"]>> = null;\n  try { process = await sandbox.getProcess(processId); } catch { /* fall through to ambiguity */ }\n  if (process && !IMPLEMENTATION_TERMINAL_STATUSES.has(process.status)) {\n    try { await process.kill("SIGTERM"); } catch { /* bounded cleanup only */ }\n    const result = { processId, processStatus: process.status, reason: "implementation_process_timeout" };\n    await journal.settleAction(id, { state: "FAILED", result });\n    throw new RunFailure("FAILED_SAFE", "implementation process exceeded the objective duration", result);\n  }\n  const result = { processId, reason: "implementation_process_terminal_state_unavailable" };\n  await journal.settleAction(id, { state: "AMBIGUOUS", result });\n  throw new RunFailure("AMBIGUOUS_EFFECT", "implementation process terminal state could not be reconciled", result);\n}\n\n`;
executor = executor.replace(helperMarker, helpers + helperMarker);

const oldImplementation = `    const repositoryGovernanceRequired = objective.objectiveClass === "maintenance_pr"\n      && await hasPackageScript(sandbox, repositoryPath, "governance:impact");\n    const prompt = implementationPrompt(objective, repositoryGovernanceRequired);\n    await runAction(\n      sandbox,\n      journal,\n      objective,\n      "implement",\n      "local_write",\n      CODEX_IMPLEMENT_COMMAND,\n      {\n        cwd: repositoryPath,\n        env: { HOME: "/workspace/home", CODEX_API_KEY: openaiApiKey, DONESTATE_OBJECTIVE: prompt },\n        timeout: objective.maxDurationMs,\n      },\n      [openaiApiKey, githubToken],\n    );\n`;
const newImplementation = `    const repositoryGovernanceRequired = objective.objectiveClass === "maintenance_pr"\n      && await hasPackageScript(sandbox, repositoryPath, "governance:impact");\n    await reconcileImplementationProcess(sandbox, journal, {\n      ...objective,\n      objectiveClass: repositoryGovernanceRequired ? "maintenance_pr" : objective.objectiveClass,\n    }, openaiApiKey, githubToken);\n`;
if (!executor.includes(oldImplementation)) throw new Error('old implementation block context drift');
executor = executor.replace(oldImplementation, newImplementation);
fs.writeFileSync(executorPath, executor);

const testPath = 'apps/mcp-worker/test/executor.test.ts';
let test = fs.readFileSync(testPath, 'utf8');
test = test.replace(
  'import { describe, expect, it } from "vitest";\n',
  'import fs from "node:fs";\nimport { describe, expect, it } from "vitest";\n',
);
test = test.replace(
  'import { CHANGED_FILES_COMMAND, CODEX_IMPLEMENT_COMMAND, PUBLIC_CLONE_MAX_ATTEMPTS, PUBLIC_CLONE_RETRY_BASE_DELAY_MS, SANDBOX_RUNTIME_OPTIONS, decodeChangedFiles, implementationPrompt, protectedMaintenancePath, publicCloneCommand, publicCloneRetryDelayMs, publicCloneSandboxId } from "../src/executor";',
  'import { CHANGED_FILES_COMMAND, CODEX_IMPLEMENT_COMMAND, IMPLEMENTATION_LOG_TAIL_BYTES, IMPLEMENTATION_PROCESS_RECONCILE_ATTEMPTS, IMPLEMENTATION_PROCESS_START_ATTEMPTS, IMPLEMENTATION_STDERR_PATH, IMPLEMENTATION_STDOUT_PATH, PUBLIC_CLONE_MAX_ATTEMPTS, PUBLIC_CLONE_RETRY_BASE_DELAY_MS, SANDBOX_RUNTIME_OPTIONS, decodeChangedFiles, implementationProcessCommand, implementationProcessId, implementationPrompt, protectedMaintenancePath, publicCloneCommand, publicCloneRetryDelayMs, publicCloneSandboxId } from "../src/executor";',
);
const testInsert = `\n  it("launches Codex once under a deterministic reconcilable process identity", () => {\n    const runId = "d295908a-633e-4201-8465-881dd8bffa2d";\n    expect(IMPLEMENTATION_PROCESS_START_ATTEMPTS).toBe(1);\n    expect(IMPLEMENTATION_PROCESS_RECONCILE_ATTEMPTS).toBe(3);\n    expect(IMPLEMENTATION_LOG_TAIL_BYTES).toBe(65_536);\n    expect(implementationProcessId(runId)).toBe(\`donestate-implement-\${runId}\`);\n    expect(implementationProcessCommand()).toBe(\`(\${CODEX_IMPLEMENT_COMMAND}) > \${IMPLEMENTATION_STDOUT_PATH} 2> \${IMPLEMENTATION_STDERR_PATH}\`);\n    const source = fs.readFileSync(new URL("../src/executor.ts", import.meta.url), "utf8");\n    expect(source.match(/sandbox\\.startProcess\\(/g)).toHaveLength(1);\n    expect(source).toContain("autoCleanup: false");\n    expect(source).toContain("await sandbox.getProcess(processId)");\n    expect(source).toContain('state: "AMBIGUOUS"');\n  });\n`;
const anchor = '\n  it("counts a complete NUL-delimited changed-file inventory without duplicates", () => {';
if (!test.includes(anchor)) throw new Error('executor test insertion anchor missing');
test = test.replace(anchor, testInsert + anchor);
fs.writeFileSync(testPath, test);

const ledgerPath = 'governance/project-ledger.json';
let ledger = fs.readFileSync(ledgerPath, 'utf8');
function updateWorkItem(id, nextAction, waitCondition) {
  const re = new RegExp(`(\\{\\n      "id":"${id}"[\\s\\S]*?"nextAction":")([^"]*)(","waitCondition":")([^"]*)("[\\s\\S]*?"evidenceIds":\\[)([^\\]]*)(\\]\\n    \\})`);
  const m = ledger.match(re);
  if (!m) throw new Error(`${id} context drift`);
  let ids = m[6];
  if (!ids.includes('"E-026"')) ids = ids.trim() ? `${ids},"E-026"` : '"E-026"';
  ledger = ledger.replace(re, `${m[1]}${nextAction}${m[3]}${waitCondition}${m[5]}${ids}${m[7]}`);
}
updateWorkItem(
  'VERIFY-006',
  'Replace only the fragile long-lived Codex exec control RPC with one deterministic single-launch Sandbox process identity, reconcile that process through read-only lookups after RPC interruption, never launch Codex twice for the same action, preserve Sandbox 0.12.9 + RPC + keepAlive + Codex 0.150.1, deploy after normal review, then launch one fresh canary.',
  'Issue 85 proved keepAlive was not sufficient: exact run d295908a-633e-4201-8465-881dd8bffa2d used aligned Sandbox 0.12.9, RPC and keepAlive, clone attempt 2 succeeded, then the admitted Codex commands.execute operation was interrupted after 23486 ms while the runtime connection was closing. Exact trace evidence shows DoneState destroy happened only after the interruption. Cloudflare 0.12.9 classifies admitted OPERATION_INTERRUPTED as non-retryable, so the execution must become reconcilable rather than retried.',
);
updateWorkItem(
  'VERIFY-004',
  'After the single-launch reconcilable Codex process repair is reviewed and deployed, run one fresh consequence-disabled maintenance canary through implementation, publication, exact-head CI, OpsTruth v2 response and terminal DoneState read-back.',
  'Issue 85 failed before publication even with aligned 0.12.9, RPC and keepAlive. The next canary must wait for the isolated process-identity reconciliation repair; Codex version, command semantics, timeout, transport, lifecycle and verifier must otherwise remain unchanged.',
);

const evidence = `    ,{\n      "id":"E-026","date":"2026-09-03","identity":"keepAlive RPC canary isolated admitted Codex operation interruption and required process identity reconciliation","situation":"PR 84 merged as 71a1188a0313ff29ddd1cb265acd53f8edc6922d and deployed Worker a645a23e-f5cf-47b6-8d7b-8c58fb78159a with Sandbox 0.12.9, RPC transport, sleepAfter 15m and keepAlive true while Codex 0.150.1, its command, timeout and retry semantics remained unchanged. Fresh issue 85 created one finding and one run d295908a-633e-4201-8465-881dd8bffa2d.","verification":"Raw Cloudflare trace ee9e1464e736c4fe2d519031bfc7d091 proves both queue records refer to the same finding/run/request; clone-1 returned process exit 128 and was destroyed, clone-2 succeeded, then the single admitted Codex commands.execute operation failed after 23486 ms with OperationInterruptedError because the runtime connection was closing. The same trace shows DoneState called Sandbox destroy only after that interruption, not before it. There was no HTTP 500, no validation, publication, branch, pull request or OpsTruth invocation. Cloudflare 0.12.9 classifies an admitted operation interruption as non-retryable. Stable process APIs expose custom processId, autoCleanup false, getProcess, terminal status and exit code, allowing a single launch to be reconciled without rerunning Codex.",\n      "accountability":{"owner":"DoneState maintainers","status":"active","nextAction":"Move only the implementation action to a deterministic single-launch Sandbox process with durable process identity, read-only reconciliation and bounded output capture. If an admitted process identity disappears, settle the action AMBIGUOUS and stop; never rerun Codex. Preserve all other runtime and verifier variables.","waitCondition":"Issue 85 is terminal pre-publication evidence and must not be retried or rewritten. Do not combine this repair with Codex upgrades, model retries, timeout changes, Sandbox version/transport/lifecycle changes, verifier changes or authority expansion.","staleDate":"2026-09-10"},\n      "outcome":"The failure is no longer modeled as a retryable Codex/API problem. DoneState needs a reconcilable process boundary so Cloudflare control-runtime interruption cannot force either duplicate execution or loss of effect identity.","content":"Exact issue/finding/run IDs, clone attempt identities and exit codes, Worker/runtime subjects, RPC/keepAlive state, 23486 ms OPERATION_INTERRUPTED, post-failure destroy ordering, absence of publication/verifier activity, and the single-launch process reconciliation contract.","measurement":"One fresh canary, one finding, one run, two bounded clone attempts with only the second succeeding, one Codex launch, one admitted operation interruption, zero HTTP 500s, zero branches, zero PRs, zero OpsTruth calls, and zero Codex retries."\n    }`;
const closeMarker = '\n  ]\n}';
const idx = ledger.lastIndexOf(closeMarker);
if (idx < 0) throw new Error('ledger closing marker missing');
ledger = ledger.slice(0, idx) + evidence + ledger.slice(idx);
JSON.parse(ledger);
fs.writeFileSync(ledgerPath, ledger);
